/* 

Exercise 1: To-Do List with Dynamic Actions

Scenario: You are building a to-do list app. Users should be able to:

a) Add new tasks.
b) Mark tasks as completed.
c) Remove tasks.

Requirements:
Add a task when the user types in an input field and clicks the "Add Task" button.

Strike through the text of a task when the user clicks on it to mark it as completed.

Add a "Remove" button next to each task. Clicking the "Remove" button deletes the task.

*/
